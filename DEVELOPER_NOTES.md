# ملاحظات المطوّر — نظام الأمراء

هذا الملف **ليس** جزءًا من واجهة النظام أو بيانات المتجر. هو سجل داخلي يوثّق
القرارات المعلّقة والمخاطر المعروفة، بحيث لا تضيع بين الجلسات المختلفة حتى لو
تغيّرت الذاكرة أو انقطع السياق.

---

## ✅ قواعد عمل إلزامية (طبّقها دائمًا، لا تخمّن)

1. **قبل أي تعديل كبير على البيانات أو الدوال** (migration، تعديل جماعي، أي
   شيء غير قابل للتراجع بسهولة): شغّل `pos_fn_backup_now()` أولاً — نسخة
   لحظية فورية (منفصلة عن النسخة اليومية التلقائية `alomraa_daily_dr`).
   موجودة ومربوطة بصفحة `backup` في الفرونت اند بالفعل، لا تحتاج بناء.
2. **قبل نشر أي استبدال كامل لنسخة صفحة** (اسم ملف جديد يحل محل قديم): عاين
   الملف الجديد فعليًا (`scripts/make_preview.py`) قبل الربط. `check_before_push.sh`
   يرفض النشر تلقائيًا لو اكتشف استبدالًا بدون تأكيد `PREVIEWED_CONFIRM=1`.
3. **بعد أي نشر**: شغّل `scripts/audit_live_site.sh` للتأكد من سلامة الموقع
   الحي. يعمل كذلك تلقائيًا يوميًا عبر `.github/workflows/daily-site-audit.yml`
   — فشله يُرسل إشعارًا تلقائيًا من GitHub لصاحب الحساب.
4. **قبل أي `git push`**: `scripts/check_before_push.sh` إلزامي دائمًا —
   يفحص secrets، ملفات preview، صحة syntax، وملفات يتيمة.
5. **عند إضافة أو مراجعة أي RPC مقصود أن يكون owner-only**: تحقق من الحماية
   في **الدالة الداخلية `pos.fn_*`**، لا في الـ `public.pos_fn_*` wrapper فقط
   — الاعتماد على إخفاء الواجهة (`isOwner ? ... : "للمالك فقط"`) بدون فحص
   `pos.current_app_role()` أو `pos.is_owner()` داخل الدالة نفسها هو بالضبط
   الخلل الذي أدى لثغرات حقيقية مؤكدة (راجع سجل القرارات المُنفَّذة أدناه).
   اختبر بمحاكاة `set_config` لمستخدم عشوائي غير owner، لا تكتفِ بالقراءة.
6. **🔴 تحذير: بعض دوال `public.pos_fn_*` ليست مجرد `SELECT pos.fn_x(...)`
   بسيطة** — عندها **جسم دالة كامل مستقل** (33 دالة من هذا النمط مؤكدة، مثل
   `pos_fn_customer_history`). في هذه الحالة، تعديل `pos.fn_x` الداخلية **له
   صفر تأثير** — لازم تعدّل `public.pos_fn_x` مباشرة. افحص طول الكود
   (`length(prosrc)`) قبل أي إصلاح: أقل من ~150 حرف يبدأ بـ`select pos.fn_` =
   wrapper بسيط آمن التعديل بالداخلية فقط؛ أطول من ذلك = لازم فحص وتعديل
   مباشر على `public.pos_fn_*` نفسها. **اكتُشف هذا بالغلطة الصعبة**: إصلاح
   `pos.fn_customer_history` بدا ناجحًا (نفس النص محدَّث)، لكن الاختبار الفعلي
   أظهر البيانات الحساسة لسه مكشوفة — لأن `public.pos_fn_customer_history`
   كانت نسخة قديمة مستقلة تمامًا لم تتأثر بالتعديل إطلاقًا.
7. **🔴 قبل أي `CREATE OR REPLACE FUNCTION` على دالة موجودة**: تأكد أن كل
   أنواع الـ parameters تطابق التعريف الأصلي **بدقة تامة** (خصوصًا الـ enum
   types المخصصة مثل `pos.payment_method` — لا تكتبها `text` عن طريق الخطأ).
   PostgreSQL لا يستبدل الدالة لو التوقيع مختلف حتى بحرف واحد في نوع واحد —
   بل **يُنشئ overload جديد بجانب القديم**، بصمت، بدون أي تحذير أو خطأ. هذا
   يعني تعديلك الجديد **لن يُستخدَم إطلاقًا** لو الاستدعاء الفعلي (عبر أي
   wrapper بينادي بنوع مطابق للقديم) لسه بيوجّه للنسخة القديمة. **اكتُشف هذا
   بالغلطة الصعبة** عند إصلاح `fn_sale` — الإصلاح بدا ناجحًا في القراءة،
   ونجح `CREATE OR REPLACE` بدون أي خطأ، لكن الاختبار الحي أظهر السلوك القديم
   الخاطئ يتكرر. الفحص الإلزامي بعد أي `CREATE OR REPLACE`:
   `SELECT count(*) FROM pg_proc WHERE proname='...' AND pronamespace='pos'::regnamespace;`
   — لو النتيجة أكبر من 1، يوجد overload يجب حذفه (`DROP FUNCTION` بالتوقيع
   الدقيق الخاطئ) قبل الاعتماد على أي اختبار.

---

## 🔴 قرارات معلّقة — تحتاج إجراءً فعليًا

### 0. 🔴🔴 اكتشاف حرج: هذا Supabase project (`mjetglnmivwphxyzflsz`) مُشترَك مع نظام آخر تمامًا
**اكتُشف عند فحص `cron.job` بحثًا عن سبب بناء ميزة تسجيل أخطاء.** الـ database
تحتوي على **50+ من الـ scheduled jobs غير متعلقة بمتجر الأحذية إطلاقًا** — أسماء
واضحة لنظام CRM/تسويق/مبيعات كامل منفصل (`meta_sync_dispatch`, `lead_scoring_run`,
`outbound_run`, `content_designer`, `axis_run_cycle`, `voice_brain_tick`, إلخ)
تعمل جميعها في schema `public` نفسه.

**ما هو آمن مؤكَّد:** جداول ودوال الأمراء نفسها معزولة بالكامل في schemas
مستقلة (`pos`, `pos_backup`) — لا تشابك مباشر في البيانات أو RLS. الـ cron job
الوحيد الخاص بالأمراء هو `alomraa_daily_dr` (النسخ الاحتياطي اليومي، معروف
ومقصود).

**ما هو غير مؤكَّد وخطير:**
- أي دالة أو جدول جديد يُبنى في schema `public` (بخلاف الـ wrapper functions
  الموجودة بنمط `pos_fn_*`) قد يتعارض اسميًا مع النظام الآخر.
- لم يُفحص فعليًا ما إذا كانت هذه الأنظمة الأخرى تستهلك موارد (CPU، اتصالات)
  تؤثر على أداء الأمراء وقت الذروة.
- **قرار بناء `cron.schedule` جديد للأمراء تم تجميده عمدًا** لهذا السبب —
  التنظيف التلقائي لجدول `client_errors` (انظر أدناه) تُرك كدالة يدوية بدون
  جدولة تلقائية، بانتظار توضيح الوضع مع المستخدم.

**السؤال المفتوح للمستخدم:** هل هذا الـ project مُشترَك عمدًا (مشروع آخر لك
أو لعميل آخر يستخدم نفس حساب Supabase)، أم أن هذا غير متوقَّع من جانبك؟
الإجابة تُحدِّد ما إذا كان آمنًا إضافة أي موارد جديدة (cron, extensions) لهذا
الـ project مستقبلاً.

### 1. تدوير GitHub PAT المكشوف
**الأولوية: قصوى، لكنها مؤجّلة بطلب صريح من المستخدم حاليًا.**

يوجد GitHub PAT مكشوف في تاريخ محادثات سابقة مع هذا المشروع (لا يُكتب هنا
صراحةً — GitHub push protection يحجب أي commit يحتوي على نص الـ token الفعلي،
وهذا صحيح ومقصود). لم يُدوَّر حتى تاريخ هذا التحديث. يتطلب إجراءً مباشرًا من
المستخدم على github.com (Settings → Developer settings → Personal access
tokens) لإنشاء token جديد وإلغاء القديم، ثم تحديث أي مكان يستخدمه (لا يوجد
حاليًا استخدام معروف له خارج جلسات هذا المساعد نفسه).

### 2. `pos_fn_items_by_date` — دالة backend جاهزة، غير مربوطة بأي واجهة
ترجع قطع مخزون منتج معيّن مُرتّبة بتاريخ الاستلام. مفيدة محتملة لصفحة
`stocktake` أو `inventory` (سيناريو "متى استلمنا هذه القطع؟"). لم تُربط بعد
لعدم وجود طلب صريح — ليست عاجلة.

### 3. رصد أخطاء العميل (client error logging) — تم بناؤه، ثم حُذف بالكامل (قرار حذر)
كان تم بناء `pos.client_errors` (جدول)، `pos_fn_log_client_error`،
`pos_fn_client_errors_recent`، و`pos.fn_cleanup_old_client_errors` بالكامل في
نفس الجلسة التي اكتُشفت فيها نقطة #0 أعلاه (مشاركة الـ database مع نظام آخر).
لم يُربط أي منها بالـ frontend إطلاقًا (صفر سجلات، صفر مراجع في أي ملف .js)،
والسؤال المفتوح للمستخدم بشأن #0 لم يُجَب عليه. بناءً على ذلك اتُّخذ قرار
**حذف كل شيء بُني لهذه الميزة** (الجدول والدوال الأربعة) بدل تركها معلّقة —
حذرًا من إضافة أي مورد جديد لـ database مشتركة الوضع فيها غير واضح، لا لأن
الميزة نفسها كانت خطأ. لو احتاجها المستخدم مستقبلاً، يُعاد بناؤها من الصفر
بعد توضيح وضع #0.

### 4. `pos.fn_exchange` — منطق محاسبي يحتاج قرار من صاحب النظام، لا إصلاح تقني بحت
**اكتُشف بقراءة عميقة، لم يحصل بعد فعليًا (0 عمليات استبدال حاليًا في `pos.exchanges`).**

عند الاستبدال، الدالة تُسجّل عمليتين منفصلتين في السجل المالي: (أ) استرداد كامل
لسعر القطعة القديمة (`pos.fn_process_return`)، و(ب) بيع كامل نقدي للقطعة الجديدة
بسعرها الكامل (`pos.fn_sale` بخصم صفر). النتيجة الرياضية: `net_profit` في تقرير
`fn_daily_ledger` لليوم يُقلَّل بمقدار **تكلفة القطعة القديمة المُستردة** أكثر من
الواقع الفعلي — لأن استرداد القطعة القديمة يُحتسب بسعرها البيعي الكامل بدون أي
تعويض عن كونها رجعت للمخزون كأصل قابل لبيع مرة أخرى.

**مثال رقمي:** استبدال حذاء (سعر 500، تكلفة 300) بحذاء (سعر 700، تكلفة 400):
- الربح الحقيقي للمعاملة ككل: `(700-500) - (400-300) = 100` جنيه
- ما يُسجَّله النظام حاليًا في نفس اليوم: `gross_profit = (700-400) = 300`،
  `returns = 500`، فـ `net_profit = 300 - 500 = -200` (خسارة ظاهرية)
- الفرق بين الاثنين = `300` جنيه (تكلفة القطعة القديمة) بالضبط

**لماذا هذا قرار محاسبي لا تقني بحت:** الإصلاح يتطلب معرفة كيف يريد صاحب
المتجر معاملة الاستبدال محاسبيًا — كصافي فرق سعر واحد، أو كعمليتين منفصلتين مع
تصحيح حساب التكلفة، أو بطريقة أخرى. لم يُنفَّذ أي تعديل بعد لعدم وجود قرار،
وبما أن الميزة لم تُستخدم فعليًا حتى الآن فلا ضرر حالي على البيانات التاريخية.

### 5. `p_actor` parameter ميت في 10 دوال — لا يحتاج إصلاح، فقط معرفة
تأكيد من القراءة العميقة: `pos_fn_add_expense`, `pos_fn_cancel_order`,
`pos_fn_close_stock_count`, `pos_fn_exchange`, `pos_fn_finalize_order`,
`pos_fn_open_stock_count`, `pos_fn_process_return`, `pos_fn_receive_stock`,
`pos_fn_reserve_order`, `pos_fn_sale` — كلها لسه عندها `p_actor uuid` في
التوقيع (والفرونت اند لسه بيمرّره من بيانات client-side)، لكن **كل واحدة من
الدوال الداخلية فعليًا بتستخدم `auth.uid()` فقط وتتجاهل `p_actor` تمامًا** —
هذا تأكيد أن الإصلاح الأمني الموثّق سابقًا في الذاكرة تم بالفعل وبدقة، والـ
parameter نفسه بقايا كود ميتة غير خطرة. لا تُصلحه بمفرده (تغيير توقيع 10
دوال + كل استدعاء frontend لها لفائدة تنظيفية بحتة لا تستحق المخاطرة) — فقط
احذفه لو كنت أصلًا بتعدّل نفس الدالة لسبب آخر.

- **تحذير هامش الربح في POS** (`pos_fn_margin_warning`): كانت مبنية بالكامل في
  الـ backend، غير مربوطة بالفرونت اند. تم ربطها بحقل الخصم في نقطة البيع —
  للـ owner فقط (الدالة ترجع boolean فقط، لا تُسرّب رقم التكلفة).
- **إصلاح CollectionsModal**: كان لا يحمّل مجموعات المنتج الحالية عند الفتح
  (يبدأ فاضي دائمًا)، فحفظه كان يمحو أي ارتباط قديم بدل تعديله. تم إصلاحه بدالة
  جديدة `pos_fn_list_collections_for_product` ترجع `is_selected` لكل مجموعة.
  اُكتشِف بقراءة الكود المصدري كاملاً سطر بسطر، لا بأي فحص تلقائي.
- **🔴 3 ثغرات أمنية حقيقية مُصلَحة** (اكتُشفت بتتبّع كل RPC حتى مصدرها الداخلي
  الحقيقي، لا بالبحث النصي السطحي فقط):
  - `pos_fn_reorder_report`: أي مستخدم مسجّل (لا owner فقط) كان يستطيع استدعاءها
    مباشرة ويرى تحليلات مبيعات/مخزون كاملة — الحجب كان client-side فقط.
    **مُثبَت عمليًا**: مستخدم عشوائي غير owner نجح في الاستدعاء قبل الإصلاح.
  - `pos_fn_product_detail`: كانت تُرجع `cost_price` (تكلفة المنتج) بدون أي
    حماية — غير مستخدمة حاليًا في أي frontend منشور (خطر كامن، صفر ضرر فعلي).
  - `pos_fn_list_customers`: كانت تُرجع `total_spent` و `tier` (بيانات مالية عن
    العميل) لأي مستخدم — أُصلحت بإخفاء هذه الحقول فقط للموظف العادي (`null`)
    بدل رفض الاستدعاء بالكامل، حفاظًا على استخدام باقي بياناته في نقطة البيع.
  كل الإصلاحات اختُبرت حيًا (owner ينجح بكل التفاصيل، موظف يُرفَض أو يرى بيانات
  محجوبة جزئيًا) مع تنظيف كامل لأي بيانات اختبار مؤقتة.
- **🔴 إصلاح ثغرة رابعة + اكتشاف منهجي حرج — `pos_fn_customer_history`**:
  كانت تُرجع `total` (إجمالي كل فاتورة سابقة للعميل) لأي مستخدم، بدون حماية.
  المحاولة الأولى للإصلاح (تعديل `pos.fn_customer_history` الداخلية فقط) بدت
  ناجحة نصيًا لكن **فشلت عمليًا بالاختبار الحي** — لأن `public.pos_fn_customer_history`
  كانت نسخة مستقلة بجسم دالة كامل خاص بها (932 حرفًا)، لا استدعاء بسيط
  للداخلية. هذا فتح فحصًا لكل الدوال (33 دالة `public.pos_fn_*` من هذا النمط
  المستقل) — باقي الدوال المفحوصة (`update_product`, `delete_product`,
  `demo_seed`, `dump_system`, `app_badges`, `search_customers`) سليمة ومحمية
  بشكل صحيح، فقط `customer_history` كانت المتأثرة. `unit_price` (سعر البيع
  الفردي) تُرك مرئيًا للجميع عمدًا بعد نقاش — سعر بيع علني لا معلومة إنفاق
  شخصية، بخلاف `total` (إجمالي الفاتورة).
- **إصلاح خامس بسيط — `pos_fn_demo_wipe`**: نفس ثغرة الجداول الأربعة المفقودة
  من النسخ الاحتياطي، لكن هنا في دالة "مسح بيانات العرض التجريبي" — لم تكن
  تمسح `collections` (الجدول المستقل فقط؛ `product_collections`/`product_images`
  محميان تلقائيًا بـ `ON DELETE CASCADE` عند حذف `products`، فلا حاجة لذكرهم
  صريحًا). أثر الثغرة كان "بقايا تنظيف" (مجموعات فاضية بلا منتجات) لا فقدان
  بيانات حقيقي — أُصلح للاتساق، لم يُختبر تنفيذيًا (دالة مدمِّرة بطبيعتها، خطر
  اختبارها على بيانات حقيقية غير مبرر لإصلاح بهذا الحجم الصغير).
- **إصلاح سادس بسيط — `pos.fn_create_product_full`**: لا حد أقصى لإجمالي
  كمية القطع عند إنشاء منتج جديد (بخلاف `fn_receive_stock` التي فيها حد 500
  قطعة/عملية) — خطأ إدخال بشري (مثل كتابة `5000` بدل `50`) كان يمكن أن يُنشئ
  آلاف الصفوف بالغلط. أُضيف حد أقصى 2000 قطعة إجمالي، **مع فحص يحصل قبل أي
  `INSERT` في `pos.products`** فلا يُنشَأ منتج يتيم بلا قطع لو رُفض الطلب.
  اختُبر حيًا: كمية طبيعية (2 قطعة) نجحت، كمية 5000 رُفضت برسالة واضحة وصفر
  أثر على قاعدة البيانات. أثناء الإصلاح اكتُشف تفصيل يستحق تسجيله: الدالة
  الأصلية لها قيم `DEFAULT` لكل الـ parameters — أول محاولة `CREATE OR REPLACE`
  فشلت لأنها حذفت هذه الـ defaults سهوًا (PostgreSQL يرفض إزالة defaults من
  دالة موجودة)؛ أُعيدت بدقة مع كل الـ defaults الأصلية.
- **🔴 إصلاح مالي حرج — `pos.fn_sale` واستبدال نقاط الولاء**: كانت الدالة
  تخصم `p_points_redeem` من رصيد العميل **بدون** أي تخفيض فعلي في `total` —
  العميل يفقد نقاطه مقابل صفر خصم حقيقي. تم إصلاح الـ backend بالكامل
  (فحص رصيد فعلي عبر `LEAST()` بدل الثقة بقيمة الفرونت اند، رفض واضح لو
  النقاط أكبر من الفاتورة)، واختُبر حيًا بثلاث سيناريوهات كاملة (استبدال
  صحيح، تلاعب بقيمة أكبر من الرصيد، تجاوز قيمة الفاتورة) بنجاح تام. أثناء
  الإصلاح الأول اكتُشفت مشكلة overload خطيرة (راجع القاعدة #7 أعلاه) —
  النسخة الأولى استُبدلت خطأً بنوع `text` بدل `pos.payment_method`، فخُلقت
  دالة موازية لم تُستخدَم إطلاقًا، وبدا الإصلاح ناجحًا في الاختبار السطحي
  رغم فشله الفعلي؛ اكتُشف وأُصلح بفحص `pg_proc` مباشرة. **في تلك الجلسة**
  عُطِّلت الميزة مؤقتًا من الواجهة (checkbox + قيمة الإرسال) لمنع تناقض بين
  الشاشة والفاتورة الفعلية، لحين إكمال تعديل الفرونت اند بعناية.
- **🔴✅ إكمال إصلاح واجهة نقاط الولاء في POS (جلسة تالية)**: أُعيد تفعيل
  الميزة بالكامل. الحل: متغيّر واحد `redeemAmount` على مستوى الصفحة، تُقرأ
  منه كل النقاط المستهلكة له (`el` المعروضة، استدعاء `fn_sale`، فحص الدفع
  المختلط، نص عرض الـ checkbox) بدل كل واحد منهم يحسب نسخته الخاصة. تحقّق
  رياضي عبر Node قبل أي تعديل جديد على قاعدة البيانات (لا حاجة لإعادة اختبار
  `fn_sale` نفسها، مؤكدة من الجلسة السابقة): سيناريو الاستبدال يطابق تمامًا
  نتيجة `fn_sale` الحية المُسجَّلة سابقًا (550 فرعي، 100 نقطة → `el=450`)،
  وسيناريوهات "بدون استبدال" و"بدون عميل" ترجع `redeemAmount=0` بلا أي تغيير
  عن السلوك القديم. `diff` عبر `SequenceMatcher` (لا القراءة البصرية لسطر
  مضغوط واحد) أكّد 6 نقاط تغيير فقط، كل واحدة مطابقة للتعديل المقصود، صفر
  تأثير عرضي على باقي الملف. **الميزة الآن مكتملة وآمنة للاستخدام الفعلي.**
- **🔴 إصلاح ثغرة فقدان بيانات في النسخ الاحتياطي**: `pos_backup.do_snapshot`
  و `pos_backup.do_restore` كانتا تتجاهلان 4 جداول كاملة (`collections`,
  `product_collections`, `product_images`, `static_files`) — لم تكن تُنسَخ
  احتياطيًا من الأساس، فلو حصلت كارثة تتطلب استرجاع نسخة قديمة، هذه البيانات
  كانت ستُفقد نهائيًا بلا رجعة. **اُختُبر الإصلاح بسيناريو كامل حقيقي**: إنشاء
  مجموعة + ربطها بمنتج → أخذ نسخة → حذف البيانات يدويًا (محاكاة فقدان) →
  استرجاع النسخة → تأكيد رجوع كل البيانات بدقة 100% (مع أخذ نسخة أمان إضافية
  قبل الاختبار نفسه، تطبيقًا للقاعدة الإلزامية #1 أعلاه). صفر ضرر على بيانات
  المتجر الحقيقية (517 قطعة / 8 منتجات) طوال الاختبار.
- **🔴✅ إصلاح جذري لشكوى "السكانر بطيء" — السبب الحقيقي كان `checkMargin`
  لا السكانر نفسه ولا الشبكة**: العميل اعترض تاني على سرعة القراءة بعد إصلاح
  إعدادات الطابعة (جلسة سابقة). قراءة كاملة لمسار "مسح باركود → إضافة للسلة"
  أثبتت أن حقل الإدخال نفسه بسيط (`onChange` عادي، صفر تكلفة لكل حرف)، وأن
  السكانر (USB، سلوكه كيبورد + Enter) يعمل بسرعته الطبيعية دون أي تدخل من
  الكود. **المشكلة الفعلية**: `checkMargin` (للـ owner فقط + خصم مفعَّل) كانت
  تعمل حلقة `for...of` مع `await` بداخلها — **استدعاء RPC منفصل لكل قطعة في
  السلة، بالتسلسل**، في كل مرة تُضاف/تُحذف قطعة أو يتغيّر الخصم — لا فقط
  للقطعة الجديدة. سلة من 5-8 قطع (شائع في محل أحذية) = 5-8 رحلات شبكة متتالية
  بعد كل مسحة، تراكم زمن يُحسّ به المستخدم كتأخير حقيقي في "السكانر" رغم أن
  القراءة نفسها فورية. **الإصلاح**: دالة backend جديدة
  `pos.fn_margin_warning_bulk(p_items jsonb)` تستقبل السلة كاملة وترجع
  boolean واحد بنفس منطق الحساب الأصلي بالضبط (مُتحقَّق سطر بسطر)، مع حد أقصى
  200 قطعة كحماية إدخال. اختُبرت حيًا بـ5 سيناريوهات (سلة عادية بلا تحذير،
  سلة فيها تحذير في قطعة غير الأولى — يؤكد فحص كل القطع لا فقط الأولى، سلة
  فاضية، منتج غير معروف، تجاوز الحد الأقصى) — كلها صحيحة، صفر أخطاء. الفرونت
  اند الآن يبني `payload` واحد ويستدعي RPC **مرة واحدة** بغض النظر عن حجم
  السلة، بدل N استدعاء. توزيع الخصم النسبي على القطع (`share`) مُتحقَّق رياضيًا
  عبر Node قبل النشر (يطابق مجموع الخصم الأصلي بدقة). مستوى الأمان لم يتغيّر
  (نفس التصميم: boolean فقط، بلا تسريب `cost_price`، متاحة لأي `authenticated`
  تمامًا كالدالة الأصلية — الحد هو حجم الطلب فقط، لا الصلاحية).
- **🔴🔴 إصلاح أخطر — Race Condition حقيقي كان يفقد قطع من الفاتورة أثناء
  المسح السريع، وهو الاحتمال الأرجح للسبب الجذري الفعلي وراء شكوى "السكانر"
  المتكررة**: بعد إصلاح `checkMargin`، تدقيق شامل لكل مسار "مسح → إضافة
  للسلة" (لا الاعتماد على الإصلاح السابق كافٍ) اكتشف أن `ee()` كانت تقرأ
  السلة الحالية (`h`) **مرة واحدة في بداية الدالة**، ثم تنتظر استعلام شبكة
  (بحث الباركود)، ثم تكتب `[...h, قطعة_جديدة]` باستخدام تلك النسخة **القديمة**
  من `h`. لو الموظف مسح قطعة ثانية قبل ما رد القطعة الأولى يوصل (طبيعي تمامًا
  بسرعة مسح حقيقية مع زمن شبكة 150-300ms)، كل استدعاء كان بيمسك نفس النسخة
  القديمة من السلة، وأيًا كان الرد اللي يوصل أخيرًا **يمحو القطعة الأخرى من
  السلة نهائيًا بلا أي رسالة خطأ**. **مُثبَت بمحاكاة Node دقيقة تكرّر نفس
  منطق الكود بالضبط**: 5 مسحات بمعدل طبيعي (80ms بين كل مسحة) بأزمنة شبكة
  متفاوتة واقعية (150-300ms) نتج عنها **قطعة واحدة فقط من 5 نجت في السلة** —
  هذا فقدان بيانات فعلي وصامت، لا مجرد بطء محسوس، وكان بيزيد سوءًا كل ما كان
  الموظف أسرع وأكثر خبرة في الاستخدام. **الإصلاح**: استخدام صيغة React
  الوظيفية لتحديث الحالة (`p(prev => ...)`) بدل قراءة `h` مباشرة، بحيث كل
  تحديث يُطبَّق على آخر حالة حقيقية وقت التنفيذ الفعلي، بغض النظر عن ترتيب
  انتهاء الاستعلامات. فحص التكرار (منع إضافة الباركود نفسه مرتين) نُقل داخل
  نفس الـ updater كطبقة حماية نهائية. أُعيد تشغيل نفس محاكاة الفشل بعد
  الإصلاح: **كل الخمس قطع نجت بالكامل**، وسيناريو "نفس الباركود مرتين بفرق
  5 مللي ثانية" رُفض بدقة (نسخة واحدة، لا صفر ولا اثنين). زر الحذف اليدوي
  للقطعة (بلا `await` أصلًا، فآمن نظريًا من الأساس لأن JS تنفيذ متزامن) عُدِّل
  بنفس النمط للاتساق فقط، لا لإصلاح خلل مُثبَت فيه. مسح الكاميرا (`onScan`)
  يستدعي نفس `ee()`، فمشمول بالإصلاح تلقائيًا بلا حاجة لتعديل منفصل. باقي
  مسارات الباركود في النظام (المرتجعات: استدعاء RPC واحد ذري بلا state محلي
  متزامن؛ توليد الباركود: `nextval()` على sequence، ذرّي بطبيعته في
  PostgreSQL) فُحصت وتأكَّد عدم وجود نفس الخطر فيها.
- **تنظيف ملفات chunks ميتة**: تم اكتشاف وحذف عدة دفعات من ملفات `.js` غير
  مستخدمة عبر جلسات متعددة (`labels`, `customers`, `pos`, `products`) — راجع
  `scripts/audit_live_site.sh` للفحص الدوري.
- **أرشيف الكود المصدري**: أُنشئ `source/` لحفظ JSX الأصلي لأي مكوّن نبنيه
  نحن (بخلاف صفحات المطوّر الأصلي التي لا يوجد لها مصدر — راجع `source/README.md`).
- **تعديل بصري على مسافة الباركود من حواف الملصق (`labels`)**: طلب صريح —
  الباركود المطبوع يلمس حواف الملصق مباشرة، والمطلوب مساحة فاضية واضحة
  حوالينه. **تمييز مهم عن جلسة سابقة**: تعديل `barH` سابقًا (تصغير الارتفاع
  الداخلي لـ SVG) **كان بلا أي تأثير فعلي على جودة القراءة** — أُثبِت رياضيًا
  أن الصورة كلها تُمدَّد بـ `width:100%;height:auto` لتملأ عرض حاوية الملصق
  دائمًا، فأي قيمة لـ `barW`/`barH` تُعطي **نفس** عرض الخط النهائي بالمليمتر
  على الورق (النسبة الداخلية للصورة ثابتة، فقط حجم العرض الكلي يتغيّر حسب
  حجم الحاوية، لا حسب هذه القيم). حساب دقيق للعرض الفعلي الحالي (X-dimension)
  على ملصق `38×25mm` (المؤكَّد كالمُستخدَم فعليًا) لباركود من 14 خانة (يُرمَّز
  بـ Code128-C ضغطي تلقائيًا لأنه رقمي وزوجي الطول): **~0.316mm** — في الحد
  الأدنى المقبول لمعايير الصناعة (0.25mm حد أدنى، 0.33mm+ مُفضَّل للسكانرات
  المتوسطة)، لا "كارثي" لكنه هش. **الحل المطبَّق**: تضييق حاوية الباركود من
  `100%` إلى `92%` من عرض الملصق (`.bc{width:92%;margin:0.5mm auto 0 auto}`
  بدل `width:100%`)، بعد اختيار هذه النسبة تحديدًا من بين عدة خيارات محسوبة
  لتعطي أثرًا بصريًا محسوسًا (~1.44mm فراغ على كل جانب) مع بقاء العرض الناتج
  (~0.291mm) قريبًا من القيمة الأصلية ولا يدخل المنطقة الخطرة (أقل من 0.25mm).
  **قرار متروك عمدًا**: تغيير الحجم الافتراضي (`38x25` → `40x25`) كان سيعطي
  نتيجة أفضل فنيًا (هامش بصري أوضح + قراءة أفضل من الوضع الحالي لا أسوأ،
  محسوب رياضيًا)، لكن هذا **قيد فيزيائي حقيقي** (مقاس رول الطابعة الفعلي
  المُحمَّل)، لا خيار عرض برمجي — لم يُغيَّر بدون تأكيد صريح أن الرول المتوفر
  يدعم هذا المقاس. `diff` كامل أكّد أن التعديل الوحيد هو قاعدة CSS واحدة داخل
  قالب HTML صفحة الطباعة (لا المعاينة على الشاشة، منطق مختلف تمامًا ومقصود
  عدم لمسه لأن الطلب كان عن الطباعة الفعلية تحديدًا).
- **🔴 اكتشاف وإصلاح Quiet Zone ناقصة — معيار Code128 إلزامي، لا اختياري**:
  اكتُشف أثناء البحث عن مواصفات سكانر المستخدم الفعلي (Datalogic QuickScan)
  للتأكد من كفاية تعديل عرض الباركود السابق. تأكيد من مصادر متعددة (Wikipedia،
  precisionid.com، توثيق Honeywell الرسمي) أن معيار Code128 **يفرض إلزاميًا**
  مساحة فاضية (quiet zone) بعرض `10x` (عشرة أضعاف عرض أنحف خط) على يمين
  ويسار الباركود — **مستقلة تمامًا عن جودة عرض الخطوط نفسها**؛ بدونها السكانر
  قد يفشل حتى في تحديد بداية/نهاية الباركود، بغض النظر عن دقة الخطوط الداخلية.
  فحص دالة `_bc()` أكّد أنها **لا تحتوي على أي quiet zone من الأساس** — عرض
  الـ SVG يساوي بالضبط `bars.length × barW`، أول خط يبدأ من `x=0` مباشرة.
  كل الـ quiet zone الفعلية المتاحة كانت تأتي فقط من `padding` الملصق الخارجي.
  حساب دقيق: المطلوب فعليًا (`10×0.291mm ≈ 2.9mm` لكل جانب) كان أكبر من
  المتاح (`~2.44mm`، من padding الأصلي 1mm + الهامش الجديد من تعديل `.bc`)
  بفارق حوالي نصف مليمتر — **هذه الفجوة موجودة من الأساس، لا نتيجة لتعديل
  هذه الجلسة، لكنها لم تُكتشف إلا الآن لأن التركيز كان على مشكلة مختلفة (عرض
  الخط) لا هذه**. أُصلحت بزيادة `padding` الملصق من `1mm` إلى `1.5mm` —
  حساب دقيق يؤكد أن هذا يغطي المطلوب بالضبط تقريبًا (`2.90mm` متاحة مقابل
  `2.82mm` مطلوبة عند عرض الخط الناتج الأضيق قليلاً `0.282mm`، لسه فوق الحد
  الأدنى لعرض الخط نفسه `0.25mm`). تأكيد أن كل الحقول النصية على الملصق
  (`.nm`, `.sub`, `.dt`) محمية بـ`overflow:hidden` و `text-overflow:ellipsis`،
  فتقليل نصف مليمتر من المساحة الداخلية يُقصِّر النص بأناقة عند الحاجة، لا
  يكسر التخطيط. `diff` أكّد التعديل محصور في قاعدتي CSS فقط (`.bc` و`.label`).

---

## ⚠️ مخاطر بنيوية معروفة (لا حل كامل متاح حاليًا)

### GitHub Pages في وضع "Legacy"
ينشر مباشرة من فرع `main` بدون build step وبدون دعم فروع/staging متعدد.
الحل العملي البديل: `scripts/make_preview.py` (معاينة محلية قبل الربط).

### لا يوجد رصد أخطاء تلقائي (error monitoring) لواجهة العميل
لو حصل خطأ JavaScript فعلي عند العميل نفسه (وقت استخدامه الحقيقي)، لا توجد
طريقة نعرف بها إلا لو أخبرنا هو بنفسه. جُرِّب حل مبني على Supabase الموجود
(بدون خدمة خارجية) ثم حُذف كاملاً — راجع القرار #3 أعلاه لتفاصيل السبب. **هذا
مختلف عن** `daily-site-audit.yml` (فحص بنيوي للملفات المنشورة، لا لسلوك
العميل الفعلي وقت الاستخدام).

### لا يوجد اختبار تلقائي (test suite / CI) بالمعنى الكامل
`scripts/audit_live_site.sh` بديل عملي جزئي (فحص بنيوي بعد النشر)، لكنه لا
يغطي المنطق الوظيفي (هل الزرار بيعمل الحاجة الصحيحة فعليًا) — فقط "هل الملفات
سليمة ومربوطة".

---

## 🔍 تشخيص نهائي: "القطعة بتضرب مرة ومحتاجة مسحة تانية" — سبب فيزيائي، لا كود

**الشكوى:** العميل وصف إن الموظف "بيلف الباركود حوالين السكانر، بيحاول يمشيه
قدامها عشان تلقط" — القطعة بتحتاج مسحتين غالبًا عشان تنجح.

**التشخيص (بعد استبعاد كل أسباب الكود بالفحص الكامل — race condition، focus،
شبكة، quiet zone — كل واحدة فيهم فُحصت بعمق في هذه الجلسة ولا واحدة تطابق
الوصف بدقة):** السكانر عند العميل هو **ليزر خط واحد** (لا Area Imager 2D
حديث) — تأكَّد هذا من المستخدم مباشرةً. سكانرات الليزر خط واحد لها قاعدة
فيزيائية صارمة: **خط الليزر لازم يكون عموديًا
(perpendicular) على خطوط الباركود، لا موازيًا ليها** — لو الزاوية غلط،
السكانر يشوف خط طويل واحد فاتح/غامق بدل تسلسل الخطوط المطلوب، فيفشل تمامًا
في القراءة، بغض النظر عن جودة الطباعة. هذا **مختلف جوهريًا** عن
سكانرات Area Imager الحديثة اللي بتقرأ من أي زاوية أو اتجاه.

**لماذا "اللف والتمرير" هو تحديدًا سبب المشكلة، لا حلها:** الموظف (بدون علمه
بالقاعدة الفنية) بيحاول بالحدس يدور القطعة عشوائيًا لحد ما الخط (بالصدفة)
يقع بالزاوية العمودية الصحيحة — وهو أسلوب استخدام صحيح لسكانرات **الليزر
القديمة omnidirectional متعددة الخطوط** أو **Area Imager**، لكنه **غلط**
لسكانر خط واحد بسيط، اللي المفروض يُستخدم بثبات يد عند الزاوية الصحيحة من
أول محاولة، لا بحركة بحث.

**الحل: نصيحة استخدام، لا تعديل كود.** يوجّه الخط الأحمر إنه يعبر الباركود
عموديًا (من فوق لتحت على الخطوط)، مع ثبات اليد لحظة واحدة بدل اللف. الكود
والطباعة (بعد إصلاحات هذه الجلسة: quiet zone، عرض الخط) سليمة ومؤكَّدة —
هذه فجوة معرفية بسيطة في تقنية الاستخدام، لا عيب هاردوير أو برمجيات.

**درس للجلسات القادمة:** قبل الحكم على "بطء" أو "عدم قراءة" الباركود كمشكلة
كود، **اسأل عن سلوك الاستخدام الفعلي بدقة** (شكل الشعاع، الحركة، الزاوية)
قبل الغوص في الكود — أسئلة بسيطة زي "الشعاع خط واحد ولا ومضة؟" ممكن توفّر
ساعات من فحص عميق في مكان غلط تمامًا.

---

## 🔴 تصحيح للتشخيص أعلاه: السبب الحقيقي كان في الكود بالفعل — bug حقيقي في حقل "اختر المنتج" بصفحة الطباعة

**التشخيص الفيزيائي أعلاه (زاوية الليزر) كان معقولًا وقت كتابته، ومعلومة
صحيحة بشكل عام عن سكانرات الليزر خط واحد — لكنه لم يكن السبب الكامل في هذه
الحالة بالتحديد.** صورة شاشة من المستخدم كشفت الحقيقة: الموظف كان بيمسح
الباركود داخل حقل **"اختر المنتج" في صفحة `labels`** (لا صفحة `pos`) —
وهذا الحقل بيفلتر قائمة منتجات محمَّلة مسبقًا بواسطة بحث نصي محلي على
`name`, `brand`, `base_sku`, `category` فقط. **الباركود (14 خانة أرقام)
لا يمكن رياضيًا أن يطابق أي حقل من هذه الحقول** — فالنتيجة كانت "لا نتائج"
دائمًا، بغض النظر عن دقة أو سرعة السكانر. هذا يفسّر بدقة تناقض المستخدم
("باركود زجاجة المياه بيتقرا بسرعة، باركودنا بطيء") — السكانر نفسه سليم
بالكامل؛ باركود الزجاجة لم يكن يُمسح في هذا الحقل الخاطئ من الأساس.

**الإصلاح:** أُضيفت دالة `tryBarcodeMatch()` تُستدعى من نفس `onChange`
الموجود، تتفعّل فقط لو النص المُدخَل أرقام بحتة بطول 6-20 خانة (لا تتعارض
مع البحث النصي العادي بالاسم/البراند/SKU). لو طابق باركودًا حقيقيًا في
`pos_items_scan`، يُستدعى `ep()` (نفس دالة الاختيار العادية) تلقائيًا.

**الدرس الأهم من هذه الحلقة كاملة:** التشخيص النظري العميق (بحث، حسابات،
مصادر صناعية) مفيد ومطلوب، **لكن لا يعوّض عن رؤية فعلية لما يحصل على
الشاشة**. صورة واحدة من المستخدم كشفت في ثوانٍ ما لم يكشفه بحث تقني عميق
عبر جلستين — لأن السؤال الحقيقي لم يكن "هل السكانر بيقرأ صح؟" بل "الموظف
بيمسح في **أي حقل** بالضبط؟". عند تكرار مشكلة مشابهة مستقبلًا، **طلب
لقطة شاشة لحظة حدوث المشكلة (لا وصف لفظي فقط) خطوة أولى رخيصة جدًا
التكلفة، وقد توفّر جلسات كاملة من التنقيب النظري.**

---

## قاعدة تحديث هذا الملف

**أي قرار جديد يُؤجَّل عمدًا (لا يُنفَّذ فورًا) يجب أن يُضاف هنا في القسم
الأحمر أعلاه فورًا في نفس الجلسة التي اتُّخذ فيها القرار.** لا تعتمد على
الذاكرة وحدها.

---

## 🔴 اكتشاف بنيوي حرج: GitHub Pages Legacy Build يفشل صمتًا عند تعديلات متلاحقة سريعة — الموقع الحيّ قد يكون مُعطَّلًا (404) دون أي إشعار

**السياق:** العميل أبلغ عن "منتجات جديدة لا تُظهر أي رد عند المسح لمدة ~30 ثانية، بلا رسالة خطأ إطلاقًا". فُحص كل مسار ممكن من جهة الكود والـ DB:
- أداء استعلامات `pos_items_scan` و`pos_items` (0.86–4.5ms على DB، وحتى تحت stress test بـ 800+ طلب متتالي: p99 ~40ms، حالة نادرة واحدة 1.3s غير متكررة)
- سلامة بيانات الباركود (لا تكرار، لا corruption، ترميز سليم)
- منطق RPC (`fn_margin_warning_bulk`) — سليم، fire-and-forget، لا يحجب الـ scan
- Supabase client instantiation — تأكَّد أن `createBrowserClient` من `@supabase/ssr` بها **singleton caching داخلي تلقائي** بالفعل (`isSingleton` يُفعَّل تلقائيًا في المتصفح عبر فحص `typeof window`) — لا حاجة لأي تدخل يدوي هنا، هذا مسار طبيعي بالكامل وليس مصدر المشكلة
- كود الفرونت اند نفسه (event handlers، لا حلقات تزامنية ثقيلة، لا blocking code)

**كل هذه المسارات سليمة تمامًا.** الفارق بين أقصى بطء أثبتناه (≤1.3 ثانية، حالة نادرة) والمُبلَّغ عنه (30 ثانية، متكرر) كان فارقًا نوعيًا لا كمّيًا — إشارة على أن السبب مكانه غير مفحوص.

**الاكتشاف الفعلي:** فحص `GET /repos/{repo}/pages` (حالة الـ Pages deployment نفسها، لا الكود) كشف `"status": "errored"`. تتبُّع `GET /repos/{repo}/pages/builds?per_page=20` أظهر نمطًا خطيرًا: عندما تُدفع عدة تعديلات منفصلة (عدة `PUT /contents/{path}` عبر GitHub API) في نفس الدقيقة أو ثوانٍ متقاربة جدًا — تحديدًا هنا: (1) إنشاء ملف JS جديد بحث جديد، (2) تحديث `pos.html` ليشير إليه، (3) تحديث `pos.txt` كذلك، (4) حذف الملف القديم — كل واحد من هذه العمليات المتتالية دخل في **queue بناء Legacy Pages بسرعة أكبر من قدرة GitHub على معالجتها التسلسلية**، فبدأ كل بناء جديد قبل اكتمال السابق، مما أدى إلى `errored` بـ `duration: 0` (فشل فوري، لم يبدأ التنفيذ حتى) لعدة commits متتالية.

**الأثر الحقيقي المحتمل:** بين لحظة نجاح بناء "الملف الجديد موجود" ولحظة نجاح بناء "الإشارة إليه في `pos.html` محدَّثة"، كان الموقع الحيّ (على الأرجح) في حالة **توقف تناقض (inconsistent state)**: HTML يشير لملف JS تم حذفه أو لم يُبنَ بعد → طلب 404 حقيقي لأصل تنفيذ الصفحة بالكامل، لا مجرد بطء استعلام واحد. هذا *لا يزال فرضية غير مؤكدة 100% بالنسبة لهذه الحادثة تحديدًا* (لم نتحقق من محتوى الصفحة الحيّة الفعلي أثناء نافذة الخطأ، لأن `github.io` خارج الشبكة المسموحة لبيئة Claude Code — راجع قسم قيود الشبكة)، لكنها الفرضية الوحيدة المتبقية بعد استبعاد كل مسار آخر بدقة رياضية.

**سلسلة الأحداث الموثّقة (للتحقق المستقبلي):**
```
built    20:22:18Z  0e70255  (إصلاح رسالة timeout 8 ثواني)
built    20:48:16Z  121c1bf  (إنشاء page-...v4cachefix.js الجديد)
errored  20:48:49Z  676a25f  (تحديث pos.html)  ← duration: 0
errored  20:48:51Z  9e28ec2  (تحديث pos.txt)   ← duration: 0
errored  20:49:11Z  f3d2df5  (حذف الملف القديم) ← duration: 0
[الموقع الحيّ متجمد على حالة build 121c1bf طوال هذه الفترة]
built    21:07:46Z  1e4f4e5  (touch .nojekyll لإعادة تفعيل queue البناء)
```
الفجوة الزمنية بين أول `errored` وآخر `built` ناجح تالٍ كانت ~19 دقيقة — خلالها الموقع الحيّ لم يعكس أي من التحديثات الثلاثة الأخيرة.

**الدرس الحاسم لكل الجلسات القادمة:**
1. **بعد أي سلسلة من أكثر من commit واحد على GitHub Pages (Legacy)، يجب دومًا التحقق من `GET /repos/{repo}/pages` (الحقل `status`) — لا يكفي التحقق من أن كل commit وصل لـ `main` بنجاح عبر `GET /contents/`.** commit ناجح على مستوى git ≠ نشر ناجح على مستوى Pages. هذا فرق جوهري كان مفقودًا في كل الفحوصات السابقة.
2. **عند أي تعديل يتطلب أكثر من ملف مرتبط ببعضه (مثل: ملف JS جديد + تحديث مرجعه في HTML) — يُفضَّل الانتظار بضع ثوانٍ بين كل `PUT` والتالي**، أو التحقق من نجاح بناء كل خطوة عبر `pages/builds/latest` قبل المتابعة للخطوة التالية، بدلًا من إطلاق كل التعديلات المرتبطة تباعًا بأقصى سرعة.
3. **إذا ظهرت `status: "errored"` دون سبب واضح من محتوى الكود نفسه، الحل الفوري هو "touch" أي ملف بسيط (مثل `.nojekyll`) بتعديل تافه (سطر فاضي) لإعادة تشغيل queue البناء** — لا حاجة لإعادة نشر التعديلات الأصلية نفسها، فهي موجودة بالفعل على `main` وستُنشر تلقائيًا بمجرد نجاح البناء التالي.
4. **هذا النمط (`errored` builds متتالية) كان موجودًا في تاريخ المشروع من قبل هذه الجلسة** (رُصد عبر `pages/builds` تاريخ اليوم نفسه في الساعات 16:44، 16:45، 17:20، 17:21، 17:51، 17:52 — أي أربع حوادث سابقة في نفس اليوم قبل هذه الحادثة) — **لم تكن ملحوظة أو مُشخَّصة في أي جلسة سابقة رغم تكرارها.** من المحتمل أن بعض "الإصلاحات التي لم تصل" في جلسات سابقة (المذكورة أعلاه في قسم Quiet Zone وبطء السكانر) كانت تعاني من نفس السبب البنيوي، لا من مشكلة في محتوى الكود نفسه.

**قرار مؤجَّل يحتاج تنفيذًا مستقبليًا:** التفكير في نقل النظام من GitHub Pages Legacy إلى استراتيجية نشر أكثر موثوقية (مثل GitHub Actions-based Pages deployment، الذي يبني بشكل تسلسلي مضمون بدل queue غير موثوق)، أو على الأقل إضافة سكريبت مساعد (`scripts/wait_for_pages_build.sh` أو ما شابه) يُستدعى تلقائيًا بعد كل دفعة تعديلات مرتبطة للتحقق من `status: "built"` قبل إعلان النشر ناجحًا لأي جلسة مستقبلية.

النتيجة العملية لهذه الجلسة: تم تأكيد أن الموقع الحيّ الآن (بعد `1e4f4e5`) متسق بالكامل — الملف الجديد موجود، `pos.html`/`pos.txt` يشيران إليه بشكل صحيح، الملف القديم غير موجود، حالة `pages: built`. **لم يُحسم بعد** ما إذا كان هذا (نافذة عدم اتساق الموقع الحيّ لمدة ~19 دقيقة) هو *بالضبط* ما وصفه العميل بـ "30 ثانية بلا رد" — التوقيتان غير متطابقين رقميًا (19 دقيقة ≠ 30 ثانية لكل عملية مسح فردية)، لذا هذا اكتشاف بنيوي مهم ومُصلَح، لكنه *قد لا يكون* السبب الجذري الكامل لشكوى العميل الأصلية. يتطلب تأكيدًا من استخدام العميل الفعلي القادم.

---

## 🟡 نمط ثانٍ لفشل GitHub Pages Legacy: توقف كامل (`building` معلّق) لا `errored` سريع — نفس العلاج، سبب مختلف

**السياق:** بعد إصلاح نمط `errored` بـ `duration: 0` (موثّق أعلاه)، ظهر نمط مختلف تمامًا لاحقًا في نفس الجلسة: بعد دفع تعديل صياغة بسيط على `diagnose.html` (commit `e405330`)، بقيت الحالة العامة لـ `GET /repos/{repo}/pages` على `"status": "building"` لأكثر من **5 دقائق ونصف متواصلة** — لا نجاح، لا فشل، توقف تام. كل البنايات الأخرى في نفس الجلسة (أكثر من 10 محاولات) استغرقت 20-40 ثانية فقط.

**الفرق الجوهري عن النمط الأول:** النمط السابق (`errored`, `duration: 0`) هو فشل فوري بسبب تضارب queue بين عدة commits متلاحقة جدًا (ثوانٍ). هذا النمط الجديد هو **توقف/تعليق حقيقي (hang)** لبناء واحد فردي، على الأرجح خلل عابر من جهة GitHub نفسها (مصادفة توقيت، لا نمط استخدام معين واضح — لم يُدفع أي شيء بسرعة غير طبيعية قبل هذا الـ commit تحديدًا).

**العلاج المُطبَّق ونجاحه المؤكَّد:** دفع commit جديد تافه (تعديل `.nojekyll` بسطر فارغ إضافي لضمان اختلاف الـ blob sha عن آخر لمسة مماثلة) **كسر التعليق فورًا** — الحالة رجعت لـ `built` خلال 15 ثانية من دفع الـ commit الجديد. يبدو أن وصول commit جديد لـ GitHub يجعله يتجاوز/يُلغي البناء المُعلَّق القديم تلقائيًا ويبدأ بناءً جديدًا نظيفًا للـ commit الأحدث، بدلاً من الانتظار حتى ينتهي (أو يتعطل) البناء العالق.

**القاعدة العملية الموحَّدة لكل من نمطي الفشل (`errored` السريع، و`building` المعلَّق):**
1. تحقق دومًا من `GET /repos/{repo}/pages` (الحقل `status`) بعد أي دفعة تعديلات، لا `GET /contents/` فقط.
2. إذا لم تكن الحالة `built` خلال ~45 ثانية من الدفع (سواء ظهرت `errored` أو استمرت `building` بلا تغيير)، الحل الموحَّد هو: **دفع commit تافه جديد فورًا** (تعديل حرف واحد في `.nojekyll` أو ملف صغير مشابه) — لا الانتظار الطويل السلبي، ولا إعادة محاولة دفع التعديل الأصلي نفسه.
3. تأكيد النجاح النهائي يتطلب مطابقة `status` العام بـ `built` **و** أن `pages/builds/latest`.`commit` يطابق فعليًا آخر commit مقصود على `main` — كلا الشرطين معًا، لا أحدهما فقط (قد يكون آخر build entry ناجحًا لكنه لـ commit أقدم بينما البناء الحقيقي للأحدث لا يزال عالقًا خلفه، كما حدث هنا تحديدًا).

**قرار مؤجَّل يزداد أهمية بعد هذه الحادثة الثانية في جلسة واحدة:** الانتقال من GitHub Pages Legacy إلى GitHub Actions-based Pages deployment أصبح أولوية حقيقية لا رفاهية — نمطا الفشل المُوثَّقان هنا (تضارب queue + تعليق كامل) كلاهما من خصائص نظام Legacy القديم غير الموثوق، ولن يتكررا بنفس الشكل مع نظام بناء تسلسلي حديث يعتمد على workflow runs منفصلة وقابلة للمراقبة والإلغاء الصريح.

---

## 🔬 محاكاة عميقة إضافية بعد نافذة build queue — استبعاد رياضي شامل لكل مسار backend، وإصلاح احترازي للمسار الوحيد المتبقي غير القابل للفحص المباشر

**السياق:** بعد اكتشاف مشكلة GitHub Pages build queue وإصلاحها، طُلب فحص أعمق وأشمل ("اختبر وحاكي وصلّح واستنتج وتعمّق" — بدون طلب أي شيء من العميل). تم تنفيذ الآتي بدقة:

**1. محاكاة تسلسل `ee()` الكامل على مستوى DB (لا استعلام منفصل):**
عبر `DO` block PL/pgSQL يُنفِّذ نفس الخطوتين بالتتابع (بحث `pos_items_scan` + عدّ `pos.items`) بالضبط كما يحدث في الكود الحقيقي، لمسح 5 قطع مختلفة بالتتابع (محاكاة فاتورة حقيقية). النتيجة: 6.4ms للأولى (تحميل cache)، ثم 0.3-0.5ms للباقي. **صفر تدهور تراكمي**، يستبعد نهائيًا أي فرضية "بطء تراكمي مع تكبّر الفاتورة" على مستوى DB.

**2. فحص Connection Pool:** 17 اتصال من أصل 60 (28.3%) — بعيد جدًا عن أي تشبّع.

**3. حساب فيزيائي/جغرافي لأقصى RTT نظري ممكن:** من القاهرة لأي من مراكز بيانات Supabase الأوروبية المحتملة (Ireland/Frankfurt/London)، أقصى RTT واقعي محتمل (مع overhead التوجيه الحقيقي 1.5-3x الحد النظري) لا يتجاوز ~250ms. **الفارق بين هذا الحد الأقصى المطلق و30,000 ثانية المزعومة هو ×120** — يستبعد رياضيًا أي تفسير جغرافي/توجيه شبكي عادي بغض النظر عن جودة الاتصال.

**4. محاولة محاكاة HTTP حقيقية من بيئة Claude Code:** فشلت بسبب حظر `supabase.co` في network egress allowlist الخاص ببيئة الجلسة (قيد بيئي حقيقي، لا قابل للتجاوز الشرعي — تم تجربة `--resolve` بـ IP مباشر لكل من عنواني Cloudflare المُكتشَفين `172.64.149.246` و`104.18.38.10`، كلاهما محظور بنفس طريقة الـ hostname). **هذا يعني: لا يمكن تأكيد أو نفي فرضيات طبقة الشبكة/DNS بشكل مباشر من بيئة Claude في هذا المشروع.**

**5. فحص TTL الفعلي لسجل DNS:** `300 ثانية` (5 دقائق)، مع عنوانين IP مختلفين (Cloudflare anycast طبيعي، لا شذوذ).

**الخلاصة المنطقية النهائية بعد استبعاد كل مسار backend بدقة قاطعة:** المسار الوحيد المتبقي المتوافق حسابيًا مع "30 ثانية صمت تام، بلا أي استجابة" هو **فشل عابر على مستوى DNS resolution أو الاتصال الأولي (TCP/TLS) من جهاز/شبكة العميل نفسه** — تحديدًا نمط "DNS cascading timeout" المعروف في أنظمة Android/متصفحات الموبايل (محاولات متتالية على resolvers مختلفة، كل واحدة بـ timeout يتراكم إلى 15-30 ثانية). **هذا لم يُؤكَّد تجريبيًا بشكل مباشر** (لا يمكن ذلك من بيئة Claude، ولم يُطلَب أي فحص إضافي من العميل بناءً على تعليمات صريحة بعدم طلب أي شيء منه) — إنما هو الفرضية الأقوى المتبقية بعد استبعاد كل بديل آخر رياضيًا.

**الإصلاح الاحترازي المُطبَّق (commit `571f055`):** استبدال `setTimeout` البسيط (انتظار سلبي 8 ثوانٍ ثم رسالة خطأ فقط) بدالة `scanLookupWithRetry()` تستخدم `AbortController` + `abortSignal()` (مؤكَّد وجودها في نسخة مكتبة Supabase JS المُحمَّلة فعليًا عبر فحص الكود المصدري المُجمَّع، لا افتراضًا) — تُلغي المحاولة الأولى قسرًا بعد 4 ثوانٍ فقط بدل الانتظار الكامل، وتُعيد المحاولة فورًا مرة واحدة. **الأساس المنطقي:** لو كان السبب فعلاً فشلًا عابرًا (لا مستمرًا)، فالمحاولة الثانية الفورية على الأرجح ستستخدم DNS resolver مختلف أو حالة شبكة قد تعافت، فتنجح في نفس المللي ثوانٍ الطبيعية (30-60ms) بدل الانتظار الكامل. الاستعلام `SELECT` فقط (بحث لا كتابة)، فآمن تمامًا من أي احتمال تكرار بيانات عند إعادة المحاولة. تمت محاكاة ذهنية لكل من السيناريو الأفضل (نجاح فوري في المحاولة الثانية) والأسوأ (فشل الاثنين، نفس السقف الزمني 8s الإجمالي كحد أقصى، بلا تراجع عن الوضع السابق) قبل النشر — كلا المسارين آمنان، لا يوجد سيناريو يُسبِّب تدهورًا عن الحالة السابقة.

**درجة الثقة الصادقة في هذا الإصلاح:** هذا **علاج احترازي مبني على الفرضية الأقوى المتبقية إحصائيًا، لا تأكيدًا مثبتًا تجريبيًا للسبب الجذري النهائي.** لا يوجد وسيلة (من بيئة Claude، دون طلب أي إجراء من العميل) لتأكيد فرضية DNS/شبكة العميل بشكل قاطع 100%. إذا استمرت الشكوى بعد هذا الإصلاح مع ظهور رسالة "بطء في الاتصال" فعليًا (لا صمت تام)، فهذا يؤكد فشلًا شبكيًا مستمرًا فعليًا (لا عابرًا) يتطلب تدخلًا على مستوى الشبكة نفسها (راوتر/مزوّد خدمة) لا الكود. إذا اختفت الشكوى تمامًا، فهذا يدعم فرضية الفشل العابر بقوة (دون تأكيد قاطع، لأن نافذة الـ cache-bust وإصلاح checkMargin السابقين قد يكونا أيضًا ساهما في النتيجة الإيجابية بشكل متزامن، ولا وسيلة إحصائية لفصل أثر كل إصلاح على حدة من ملاحظة واحدة).

---

## 🔒 الفحص الشامل النهائي "قفل كل حاجة" — 03-07-2026 (جلسة الإغلاق)

طُلب فحص شامل نهائي بكل الاتجاهات قبل اعتبار النظام مقفولًا. النتائج والإصلاحات بالكامل:

### أ) الواجهة — جرد كامل لكل الملفات المنشورة (98 ملف)
- **إصلاح تباعد صفحة الملصقات (labels):** كان `labels.html` (preload) و`labels.txt` (RSC) يشيران لـ `page-...v7reprint.js` بينما RSC payload الفعلي داخل الملف يشير للـ canonical `page-b18e79d18394cdcd.js`. الملفان كانا متطابقين بايت-ببايت (نفس blob SHA) فلا كود ضائع — لكن التباعد كان يسبب تحميلًا مزدوجًا وخطر انقسام مستقبلي. تم التوحيد على canonical في 3 خطوات منشورة ثم حذف v7reprint نهائيًا (commits `1b18889`→`2b15541`→`c0156b7`→`4b8e5bf`). تحقق نهائي: كل الإشارات canonical، القديم 404.
- **صفر إشارات مكسورة وصفر تباعد** في كل 20 HTML + 18 TXT بعد الإصلاح. كل chunks المشتركة موجودة ومتناسقة.
- يتيم ظاهري وحيد `_not-found/page-bcb12f1bc48cbb69.js`: **يُترك عمدًا** — بنيوي لـ client-side routing في Next.js (لا تحذفه أبدًا؛ 404.html تستخدم chunk مختلفًا).
- درس regex: `[a-z-]` لا يلتقط `_not-found` — استخدم `[a-z_-]` في أي فحص مستقبلي.

### ب) الأمان — فحص أسرار على الـ 98 ملف المنشورة
صفر PATs، صفر service_role keys، صفر مفاتيح خاصة/كلمات سر، صفر sk-* keys. ✅

### ج) قاعدة البيانات — فحص RLS/policies/سلامة
- 23 جدول pos كلها RLS ON مع policies سليمة. الجداول الحساسة (product_costs/sale_profit/expenses) → owner فقط عبر `pos.is_owner()`. items/sales: قراءة تشغيلية للمصادَقين، **لا policies كتابة** = الكتابة حصريًا عبر ~70 دالة SECURITY DEFINER.
- `pos_backup` بلا RLS لكن **آمنة**: anon/authenticated بلا USAGE على الـ schema أصلًا.
- سلامة بيانات: صفر يتائم في items/costs/sale_items، صفر باركودات مكررة، صفر منتجات بلا تكلفة.

### د) إصلاح بيانات: سجل إرجاع محاسبي مفقود (INV-000054)
قطعة `00620000002024`: الإرجاع تم فعليًا 27-06 (حركة مخزون موثقة + `sale_items.returned=true` + القطعة `in_stock`) لكن صف `pos.returns` لم يُسجَّل (7 من 8 أشقائها مسجلون) → 550 ج.م refund كانت ناقصة من التقارير. **تم ترميم الصف** بنفس توقيت الحركة الفعلية ونفس المعتمد، مع reason صريح يوضح أنه ترميم. remaining_inconsistencies=0.
**الدرس:** نسخة قديمة من مسار الإرجاع لم تكن atomic بالكامل. النسخة الحالية من `pos_fn_process_return` مؤكدة atomic بالـ smoke test أدناه. أي فحص دوري مستقبلي: قارن `sale_items.returned=true` مقابل وجود صف `pos.returns` مطابق.

### هـ) Smoke test آمن للدوال الحرجة على النظام الحي — النمط الموثق
دورة كاملة (بيع → إرجاع → margin_bulk) بمحاكاة JWT المالك، داخل `DO` block ينتهي بـ `RAISE EXCEPTION 'SMOKE_OK|%', results` — **الاستثناء يعمل rollback كاملًا (صفر بيانات تجريبية تلمس النظام الحي) والنتائج تُقرأ من نص الخطأ.** النتائج: بيع 550ج ✅ (status→sold)، إرجاع كامل ✅ (refund صحيح، status→in_stock)، margin_bulk=false صحيح. تحقق لاحق: صفر أشباح (sale/return/movements).
```sql
-- النمط: perform set_config('request.jwt.claims', '{"sub":"<owner-uid>","role":"authenticated"}', true);
-- execute 'set local role authenticated';  ثم الاختبارات، ثم raise exception بالنتائج
```

### و) إصلاح فجوة ترقيم الفواتير
الـ smoke tests التاريخية (بما فيها rollbacks) استهلكت أرقامًا من `pos.sale_code_seq` دون أن تُسترجع بالـ rollback (سلوك sequences الطبيعي): آخر فاتورة حقيقية INV-000054 لكن الـ sequence كان على 70. **تمت إعادته لـ 54** — الفاتورة القادمة INV-000055 متتابعة بلا فجوة يلاحظها العميل. `barcode_seq` سليم (2444 = آخر باركود فعلي).
**قاعدة مستقبلية:** أي smoke test بيع (حتى مع rollback) يستهلك رقم فاتورة — أعد ضبط الـ sequence بعده: `select setval('pos.sale_code_seq', (select max(substring(code from 5)::int) from pos.sales), true);`

### ز) إضافة للعميل: دليل استخدام عربي شامل
`guide.html` في جذر الموقع (commit `bd5acad6`, built ✅, مطابقة بايت-ببايت) — 14 قسمًا يغطي كل عمليات المحل اليومية بمصطلحات الواجهة الفعلية (مستخرجة من الـ bundles المنشورة نفسها)، RTL/Tajawal/هوية indigo، أكورديون native بـ `<details>` بلا أي JavaScript، قابل للطباعة، `noindex`. **ملف مستقل تمامًا — صفر لمس للـ bundles.** الرابط: `<site>/guide.html`.

### ح) تنبيه أمني معلّق (مسؤولية Mohab)
**الـ GitHub PAT المستخدم في الجلسات متسرب في سجلات المحادثة ويجب عمل rotation له فورًا** من GitHub → Settings → Developer settings → Personal access tokens.

### الحالة النهائية
كل ما يمكن فحصه وإصلاحه من بيئة التطوير تم: واجهة نظيفة بلا تباعد، أمان نظيف، DB سليمة policies وبيانات، دوال حرجة مختبرة على الحي بلا أثر، ترقيم فواتير متتابع، سجل محاسبي مرمَّم، ودليل استخدام للعميل. المتبقي خارج نطاق الكود: rotation للـ PAT، وسلوكيات الأجهزة الطرفية (ماسح/طابعة/شبكة العميل) الموثقة حلولها في guide.html وفي الأقسام أعلاه.

---

## 🔺 قاعدة الأسطح الثلاثة (اكتشاف 2026-07-10 — حرج)

أي إعادة تسمية لـ chunk صفحة يجب أن تحدّث **ثلاثة أسطح** وإلا انقسم النظام لنسختين:

1. **`<script src>`** في `<route>.html` (الـ preload)
2. **الـ RSC payload** داخل نفس `<route>.html` (سلسلة `a:I[...]` — تحدد ما يُنفَّذ عند التحميل المباشر)
3. **`<route>.txt`** في جذر الريبو — **الـ RSC payload للتنقل الداخلي**: أي انتقال client-side عبر Link يجلب `/<route>.txt` وليس الـ html، ويحمّل الـ chunk المذكور فيه.

**الحادثة**: من بداية نظام الترقيم اليدوي (`-vN`) حتى 2026-07-10، كانت ملفات `.txt` مهملة كليًا:
التنقل الداخلي كان يحمّل نسخًا قديمة (inventory بدون تعديل الفاريانتات، dashboard/settings/reorder/stocktake v0)،
و`products.txt` كان يشير لملف محذوف (`v4fixed`) = ChunkLoadError عند التنقل، وكل الـ 17 txt كانت تشير لـ
`error-...js` المحذوف. هذا يفسّر أي سلوك "أحيانًا الإصلاح مش ظاهر".
تم إصلاح 22 مرجعًا في commit واحد، وأُضيف **فحص 5** في `scripts/check_before_push.sh` يمنع تكرارها آليًا.

## 🛡️ Shims توافقية (تحديث 2026-07-10 مساءً — تلغي الحذف المؤجَّل)

بدل انتظار موت الكاش: **أسماء الملفات القديمة أُعيدت كتابتها بمحتوى النسخ الحية** (بايت-بايت).
أي عميل كاش الراوتر عنده txt قديم (جلسة PWA مفتوحة من قبل إصلاح الأسطح) ينفّذ الآن الكود المُصلَح
تحت الاسم القديم. حادثة الداشبورد المعلّقة («جارٍ التحديث» الأبدي بلا خطأ ولا تليمتري) كانت بصمة
v0 القديم (raw await بلا catch) قادمًا من كاش الذاكرة. الـshims: dashboard/reorder/settings/stocktake
v0←v2، inventory v0/v2/v3←v4، error القديم←v2، products v4fixed←v15 (كان محذوفًا = ChunkLoadError).
webpack يسجّل بمعرّف الـchunk الداخلي فالتحميل المزدوج آمن (dedupe). **لا تحذف هذه الملفات** —
حجمها زهيد وهي صمّام أمان دائم للكاش القديم.

## 🗑️ ~~حذف مؤجَّل (فترة سماح CDN)~~ — أُلغي لصالح الـshims أعلاه

الـ chunks التالية أصبحت يتيمة بعد إصلاح الـ txt لكنها أُبقيت عمدًا دورة نشر واحدة
(عملاء عندهم txt قديم في كاش CDN/المتصفح قد يطلبونها خلال دقائق الانتشار). **احذفها في الجلسة القادمة**:

- `app/(app)/dashboard/page-0b4c2df091d1dc3b.js`
- `app/(app)/inventory/page-c89b8f072a6658d0.js` + `-v2.js` + `-v3.js`
- `app/(app)/reorder/page-0260dc67d025f236.js`
- `app/(app)/settings/page-dfd42db6965a12ad.js`
- `app/(app)/stocktake/page-7c7c3aba4e1644e9.js`

(حُذف فعليًا في نفس الـ commit: products v9–v13 + `enhancements/omraa-plus.js` — لم يكن يشير إليها أي شيء إطلاقًا.)

---

## 🔒 فئة حوادث: التعليق الصامت (توثيق 2026-07-10 مساءً)

**العَرَض**: سبينر أبدي، لا رسالة خطأ، لا تليمتري. **السبب الجذري**: طابور قفل المصادقة
الداخلي في supabase-js — طلب تجديد توكن لا يستقر أبدًا (iOS PWA بعد تعليق النظام) يسدّ
الطابور فتتجمد كل طلبات rpc/from في كل الصفحات بلا رفض ولا حلّ.

**الدفاعات المنشورة (ثلاث طبقات)**:
1. **الجذر — chunk 949 (module 51365)**: العميل المشترك يُنشأ الآن بـ `global.fetch` يفرض
   مهلة 10 ثوانٍ **على `/auth/v1/` فقط** (AbortController + تسجيل `auth.watchdog` في التليمتري).
   إجهاض التجديد المعلّق = تحرير الطابور = كل الصفحات تتعافى بخطأ واضح بدل التجمد.
   طلبات REST التجارية غير مقيدة عمدًا (استعادة النسخ الاحتياطية قد تطول).
   ⚠️ عُدّل **في المكان** بلا rename: chunk مشترك بين 21 صفحة، وSW network-first يضمن الانتشار ≤ 10 دقائق.
2. **dashboard v3**: سباق 8 ثوانٍ حول rpc الإحصائيات + fallback يقرأ التوكن من
   localStorage وينادي REST مباشرة (نفس مسار التليمتري المحصَّن) — دفاع في العمق.
3. **Shims**: كل أسماء chunks القديمة تحمل محتوى النسخ الحية بايت-بايت (انظر قسم الـshims).

**قاعدة عامة**: أي await لعملية شبكة في الواجهة يجب أن يستحيل عليه التعليق الأبدي —
إمّا مهلة عند المنبع (العميل المشترك) أو watchdog موضعي. عند إضافة مسارات fetch جديدة التزم بهذا.

**Backlog تليمتري**: pos/returns/backup/suppliers/customers كاتشاتها محلية (toast) بلا تسجيل
عن بُعد — إضافة `__omraaLog` لكاتشات مسارات الفلوس تتم في جلسة مخصصة بحذر، ليست عاجلة
(طبقة auth.watchdog العالمية تغطي فئة التعليق).

## 2026-07-10 — بيع بالاسم من شاشة POS (v5)
- خانة المسح أصبحت مزدوجة: أرقام = باركود (كما هي حرفيًا)، حروف عربي/إنجليزي + Enter = بحث بالاسم/البراند/SKU.
- RPCs جديدة (SECURITY DEFINER، authenticated فقط، مقفولة عن anon):
  - `pos_fn_pos_search(p_q)` → منتجات + variants {size,color,n} للمتاح فقط، حد 8.
  - `pos_fn_pick_item(p_product,p_size,p_color,p_exclude)` → نفس envelope الـ scan_item حرفيًا (FIFO + استبعاد قطع السلة).
- اللوحة: chips مقاس·لون(عدد)، الضغط يضيف للسلة بنفس مسار السكان (dup-guard، تحذير آخر قطعة، checkMargin)، العدّاد ينقص محليًا بعد كل إضافة.
- chunk: page-f8625146216a06a6-v5.js (+shim v4cachefix)، الأسطح الثلاثة محدَّثة.

## 2026-07-11 — إصلاح بانر «إعادة الطباعة»: رقم 187 كان ثابت مكتوب، مش محسوب
- الاكتشاف: البانر في صفحة الملصقات كان دايمًا يعرض "187 صنف" بغض النظر عن الداتا الحقيقية —
  الرقم كان hardcoded جوّه الـJSX من جلسة بناء الميزة (1 يوليو)، نُسخ بالخطأ من رقم مراجعة
  30 يونيو (187 قطعة بباركود عربي قديم، مختلفة تمامًا وغير مرتبطة).
- التحقق من الداتا الحقيقية وقت الإصلاح: منتج واحد فقط (صندل طبي جلد طبيعي) و5 قطع فقط
  محتاجة إعادة طباعة فعليًا (اتعلّمت 10 يوليو — حديثة وشرعية، مش أثر قديم من أي مسح/إضافة).
- الإصلاح:
  - `pos_fn_reprint_products()`: بقت ترجّع `{product_id, qty}` بدل `{product_id}` بس (GROUP BY + count).
  - الفرونت: state جديدة `eRq/eRqs` بتحسب مجموع `qty` من نتيجة الـRPC وتعرضه بدل الرقم الثابت،
    وتتصفّر بعد الطباعة والمسح زي `eR`.
- نفس اسم الـchunk (page-b18e79d18394cdcd.js) — لا حاجة لتحديث html/txt (الأسطح متوافقة أصلاً).

## 2026-07-11 — فحص شامل بعد المسح وإعادة الإضافة: آثار مسح + بجّة تانية
بطلب المستخدم بعد اكتشاف بانر 187: فحص كامل للداتابيز (30 علاقة FK) وكل ملفات الواجهة.

**آثار المسح — نضيفة 100%:**
- الباركود العربي القديم (187 قطعة، مراجعة 30 يونيو): 0 صف — اتمسح بالكامل.
- barcode_aliases من توحيد الباركود (7 يوليو): 0 صف — اتمسحت مع المسح، صفر يتيم.
- فحص آلي لكل الـ30 foreign key في schema pos: 0 orphan في أي جدول.
- كل المنتجات الـ11 الحالية اتضافت 8-10 يوليو (صفر أقدم) — إعادة إضافة نضيفة فعلاً.

**بجّة تانية بنفس نمط "187 الثابت" — اتصلحت:**
- `pos_backup.integrity_check()`: فحص `DUP_BARCODE` كان مكتوب *قبل* قرار توحيد الباركود
  (7 يوليو)، فكان بيرفع "150 تكرار severity:high" رغم إن كله سليم ومقصود (باركود واحد
  لكل منتج+مقاس+لون). اتصلح ليفحص التعارض الحقيقي بس (باركود واحد بين منتجات/مقاسات/
  ألوان مختلفة فعليًا) — الكود الجديد `DUP_BARCODE_CROSS_VARIANT`.

**مشكلة حقيقية باقية (محتاجة قرار من Mohab، مش تقنية):**
- منتج "طبي جلد طبيعي" (id 8155efd0…, اتضاف 9 يوليو، 30 قطعة في المخزون، سعر بيع 750)
  بدون صف في `pos.product_costs` — الربح المحسوب عليه في كل التقارير/الداشبورد غلط
  (بيفترض تكلفة = صفر). محتاج سعر التكلفة الحقيقي منه لإدخاله.

## 2026-07-11 — بانر تنبيهات عام على الداشبورد (فحص سلامة الداتا مستمر)
بطلب المستخدم بعد اكتشاف بجّة الـ187: آلية عامة غير مزعجة لأي مشكلة داتا مشابهة مستقبلاً،
بدل ما نحتاج جلسة فحص يدوي كل مرة.

- الداشبورد بيستدعي `pos_fn_integrity_check()` بصمت عند التحميل (owner فقط، fire-and-forget،
  لا يؤثر على أي loading state موجود، ولا يعرض حاجة لو مفيش ملاحظات).
- لو فيه ملاحظات: سطر واحد هادئ رمادي "⚠️ N ملاحظة على البيانات" — **مطوي افتراضيًا**، مش
  بانر ملوّن مزعج. الضغط عليه يفتح تفصيل كل ملاحظة بلونها حسب الخطورة (أحمر=high،
  كهرماني=medium، رمادي=low) والوصف والعدد.
- الدالة `pos_backup.integrity_check()` (المصلَّحة اليوم أيضًا من بجّة DUP_BARCODE) هي نفس
  الأداة اللي هتكشف أي مشكلة داتا جديدة تلقائيًا (تكلفة ناقصة، فاتورة بإجمالي غلط، عميل
  بنقاط سالبة، حجز طلب معلّق فوق 48 ساعة، إلخ) — 15 فحص مختلف، وهتظهر هنا أوتوماتيك من
  غير ما حد يطلب فحص يدوي.
- chunk: page-0b4c2df091d1dc3b-v3.js (نفس الاسم، تعديل داخلي فقط — لا تغيير في الأسطح).
